{{- if .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "k8-agentgate.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "k8-agentgate.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ .Values.istio.hostname }}
  gateways:
    - {{ if .Values.istio.gatewayName }}{{ .Values.istio.gatewayName }}{{ else }}{{ include "k8-agentgate.fullname" . }}{{ end }}
  tls:
    - match:
        - port: {{ .Values.istio.port }}
          sniHosts:
            - {{ .Values.istio.hostname }}
      route:
        - destination:
            host: {{ include "k8-agentgate.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}
